class MaskedAddress{
    
    // const maskAddress = (address) => {
    //     let visibleChars = Math.max(2, Math.floor(address.length * 0.2)); // Show 20% of address
    //     return address.slice(0, visibleChars) + '...' + address.slice(-visibleChars);
    //   };
      // + ' ✨✨ ' +
    //   console.log(maskAddress('abcdefghuik')); // "abc...uik"
    //   console.log(maskAddress('0x123456789abcdef123456789abcdef12345678')); // "0x1234...5678"
      
}